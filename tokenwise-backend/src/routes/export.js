const express = require('express');
const router = express.Router();

// Placeholder export routes
router.get('/', (req, res) => {
  res.json({ message: 'Export endpoint - coming soon' });
});

module.exports = router;
